<?xml version="1.0"?>
<archive>
    <name>${ProductName}-${VersionSemantic}</name>
    <files>
        <file
            src="${DirWorkspace}\readme.md"
            target="" />
        <file
            src="${DirTemp}\${ImageName}\**\*.*"
            target="" />
    </files>
</archive>
